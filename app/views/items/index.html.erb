<p style="color: green"><%= notice %></p>

<h1>Items</h1>

<div id="items">
  <% @items.each do |item| %>
    <%= item.text %>
  <% end %>
</div>

<% if user_signed_in?%>
  <%= link_to "New item", new_item_path %>
<% end %>
